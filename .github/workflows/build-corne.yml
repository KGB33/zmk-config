on: 
  push:
    paths:
      - 'corne/**'
      - '.github/workflows/build-corne.yml'
  workflow_dispatch:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix_path: corne/build.yaml
      config_path: corne/config
